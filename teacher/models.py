from django.db import models
from django.contrib.auth.models import User
from student.models import Course, Student, Assignment, Exam

# Teacher-specific models can be added here if needed
# For now, we'll use the existing models from student app
